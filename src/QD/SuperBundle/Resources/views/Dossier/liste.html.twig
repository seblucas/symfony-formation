{% extends '::base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" type="text/css" href="{{ asset('bundles/qdsuper/css/style.css') }}">
{% endblock %}

{% block title %}Liste dossier{% endblock %}

{% block body %}
    {{ dump(liste) }}
    <ul>
    {% for dossier in liste %}
        <li>
            {{ dossier[0].titre}} ({{ dossier["nombre"] }})
        </li>
    {% endfor %}
    </ul>
{% endblock %}